# Fundamental Value Historical Validation Stage 7C Preparation

Date: 2026-07-31

## Gate decision

Preparation is complete only as an offline, outcome-blind feasibility gate.
Network access remains unauthorized. The exact 310-security source, executable
request matrix, daily outcome paths, and usable 34-operand historical assembly
are blocked, so Batch 0 must not run.

## Universe

The existing 300-name current S&P fixture is explicitly insufficient. It does
not provide the durable public security, issuer, and listing identities,
historical ticker intervals, delisted/acquired/failed coverage, or 3/4/3
sector-capitalization strata required by the frozen contract.

The source mechanism requires one content-hashed snapshot with stable identity,
ticker/MIC validity intervals, GICS effective and availability lineage,
capitalization observation, lifecycle and role, and source row hashes. Until
that exists the status is `FROZEN_UNIVERSE_SOURCE_BLOCKED`. Any later result is
`CURRENT_UNIVERSE_RETROSPECTIVE` and retains an explicit survivorship
limitation. A single EODHD universe request is prepared at retry zero and
physical/weight ceiling one, but `networkAuthorized=false`.

## Frozen predictors

The four validation-only mappings are frozen from Stage 2 outputs:

- company quality: `company_quality.score`, higher is better;
- security attractiveness: `margin_of_safety.low`, higher is
  better under applicable mature-company and valid-field eligibility;
- expected return: `expected_return.central`, higher is better;
- downside risk: `downside_risk.score`, lower is better.

The central margin of safety is diagnostic only. Security attractiveness and
expected return are both bound to the Stage 2 three-year projection for the
756-session primary outcome; one- and two-year outcomes remain diagnostic.
The typed resolver reads snake-case fields from `FundamentalValueAssessmentV1`
and resolves binary conditions by their condition code.

Each mapping binds the Stage 2 model, formula, assumption, and aggregation
versions, exact source path, eligibility, direction, binary condition paths,
and content hash. Risk cap and composite scores are excluded.

## Operand audit and canary ruling

The audit enumerates all 34 Stage 3 requirements. Seven direct price or
fundamental mappings have controlled-cache candidates, but those caches do not
establish complete historical PIT availability and none closes the 24 required
derived/policy gaps. The production producer registry remains empty. Every
required operand therefore remains terminal `MISSING` for this historical
gate; current values are not relabelled as historical and no value is imputed.

No endpoint set can create a usable generic assessment under the accepted
contracts. `canaryEndpointSet` is empty and the controller must stop before any
request.

The outcome-blind feasibility audit keeps two matrices separate. The raw
source matrix hash-verifies the existing SEC v4 manifest and Tier 2 PIT
reconstruction, including accepted/available-time lineage. That controlled
Tier 2 artifact covers its own 55-security cohort at 2021-07-20, 2023-07-21,
2024-07-23, and 2025-07-25; it does not cover the Stage 7 frozen Q2 nodes or
the Stage 7 100/310 populations. The SEC manifest contains 223 security rows,
216 built timelines, and seven missing caches. These facts demonstrate raw
primitive availability only, not an accepted Stage 7 producer or target.

Separately, the accepted-target matrix verifies
`practical-long-horizon-provider-backtest-coverage-v1-3.json` at file SHA-256
`0ACBC5D9C28037D6EB3B37A4F36198143CCE7279B5204425D8442620EB84D0DE`.
Across each of nine frozen Q2 date strata and all four targets, all 100
securities remain `MISSING` because there is no accepted Stage 7 historical
producer (36 target/date cells, zero admitted components). This is an accepted
producer-coverage result, not a claim that the raw values are absent.
Candidate retrospective producers retain ordered-parent hashes and
`CURRENT_REVISION_APPROXIMATION` lineage. Shareholder-distribution coverage
also requires actual dividends and repurchases plus a free-cash-flow
denominator; operating cash flow and capital expenditure alone are
insufficient. Policy evidence is never synthesized.

Expected return additionally depends on conservative growth and net
distribution yield. Downside risk depends on governed financial resilience
(net debt to EBITDA, interest coverage, current ratio, and diluted-share
growth) as well as cyclicality, concentration, and event risk. Dilution uses
diluted weighted-average shares. Net debt to EBITDA requires governed TTM EBIT
plus depreciation and amortization; provider-native EBITDA with unproved
duration semantics is not accepted.

Therefore useful target/date coverage is not established, scaling to 310 is
not justified, and bounded acquisition is a no-go. Any future target-component
retrospective result is limited to `DEVELOPMENT_OBSERVED`; it is not a full
assessment, strict PIT support, backtest support, or forward support.

## Outcome paths and execution

The path contract freezes first-post-cutoff entry, 252/504/756-session exits,
equal-weight buy-and-hold portfolios, split adjustment, dividend reinvestment,
terminal delisting/acquisition cash outcomes, USD-only handling, explicit
liquidity costs, SPY primary comparison, dated sector ETFs, portfolio MDD,
downside capture, severe loss, and stress veto. Calculation remains blocked
until daily paths are supplied.

The exact execution matrix cannot be materialized before the 310 durable IDs
are frozen. Its required cardinalities and controls are sealed, including 322
Yahoo baseline wrapper identities, 930 EODHD baseline requests at weight 3720,
optional 310/310 EOD and market-cap phases, 36/36 benchmark actions, retry zero,
leases, endpoint intents, UNKNOWN refusal, checkpoints, registry receipts,
Batch 0 reuse receipts, and the 25-security ceiling. It remains
`BLOCKED_EXECUTION_CONTRACT_INCOMPLETE` and cannot send transport.
