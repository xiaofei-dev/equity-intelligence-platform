# Development Log — 2026-07-29

## Forward DQV V18 machine-verifiable acceptance

- Added a Git-safe V18 acceptance artifact and offline verifier that bind the
  migration, v2.1 outcome contracts, and PostgreSQL repository by SHA-256.
- The verifier enforces the exact seven analytics tables, five
  completed-session horizons, six benchmark kinds, PostgreSQL 17 clean and
  upgrade-path matrix, and recorded repository/full-suite/Ruff/diff evidence.
- Acceptance explicitly separates implementation readiness from execution:
  the V18 implementation is ready, while prospective enrollment and outcome
  computation remain not executed.

## Objective current replay recovery

- Added a zero-network cached EODHD Fundamentals replay that validates
  content-addressed responses before writing current company-profile,
  market-capitalization, and classification projections.
- Replayed 44 securities into PostgreSQL with 44 immutable audit events.
  Re-execution wrote zero new business rows.
- Added supplemental replay validation for provider identity, source marker,
  controlled storage hash, profile projection, market capitalization,
  classification, and zero-network audit evidence.
- Preserved the original source snapshot and derived a later cutoff only from
  validated supplemental evidence.
- Stopped the first enriched replay transactionally when an `INCLUDED`
  security lacked current market capitalization. No corrected snapshot or run
  was partially committed.
- Identified the exact remaining ordinary-company repair set: AAPL, ABT, ACN,
  CAT, COST, EXPO, JNJ, MDT, NEE, PEP, and TMO.
- Rejected a zero-network price-times-shares shortcut because the stored
  `shares_outstanding` operands for all 11 are explicitly `NOT_VERIFIED`.
- Versioned the corrected replay as
  `objective-current-gate-replay-v1.1.0`.
- Kept all 66 members in the cloned snapshot while limiting Objective coverage
  rows to the 55 `INCLUDED` members. Reference-only and excluded securities
  retain explicit membership and `NOT_APPLICABLE` product views without an
  invented size cohort.
- Reused V1-V17; no V18 migration was added.
- Preserved the superseded v1.0 snapshot and screening run as append-only,
  non-authoritative historical evidence.
- Verified Python Ruff, 541 Python tests with 13 environment-gated skips, and
  five isolated PostgreSQL 17 replay tests. The isolated test database was
  removed after verification.
- Confirmed `.env` is ignored and untracked. No provider request, commit, push,
  cloud resource, scheduler, or deployment was created.

## Historical decision-quality validation foundation

- Added a pure offline cross-sectional time-slice evaluator for frozen
  deterministic model scores.
- Added explicit PIT-verified and conservative-lag evidence modes, historical
  and retrospective universe modes, chronological partitions, and claim caps.
- Added 5-, 20-, 60-, and 252-session outcome support, market or sector
  benchmark comparison, round-trip costs, boundary-tie buckets, Spearman rank
  information coefficient, top-minus-bottom spread, hit rate, coverage, and a
  deterministic diagnostic bootstrap interval.
- The evaluator rejects future input availability, future universe membership,
  same-time outcome entry, duplicate securities, and duplicate horizons.
- Added six contract tests covering look-ahead rejection, strong positive,
  approximate-evidence claim caps, unfavorable rank direction, insufficient
  sample, and sector-benchmark missingness.
- Documented the first practical workflow: run the price-only tactical
  walk-forward, reconstruct monthly Objective slices using actual availability
  or conservative lag policies, keep an untouched chronological holdout, and
  later add dated membership and delisted securities.
- No model formula, weight, cohort threshold, missing-data rule, database
  migration, provider request, score run, commit, push, cloud resource, or
  deployment was created by this foundation step.
- Ran the first zero-network tactical diagnostic over all 55 primary and
  reserve securities using 260 locally stored SPY sessions from 2025-07-16
  through 2026-07-28. Adjusted OHLC was reconstructed by applying the
  adjusted-close/raw-close factor to raw OHLC. The 333 episodes produced negative average
  cost-adjusted SPY-relative returns at 5, 20, and 60 sessions, so the frozen
  result is `UNFAVORABLE_RECENT_PERIOD`, not a positive edge claim.
- Sealed that observed period as development evidence so it cannot be reused
  as an untouched holdout after model changes.

## Expanded historical time-slice validation

- Added deterministic, versioned sampling across recent 3-9 month, medium
  1-3 year, and older 4-10 year bands.
- Sealed six random dates per band with seed `20260729` and 105 month-end
  decision dates before evaluating outcomes.
- Added maturity flags for 5-, 20-, 60-, 126-, and 252-session outcomes and
  corrected the exact-horizon boundary.
- Collected a bounded Yahoo development cache for 55 primary/reserve
  securities plus SPY from 2014-01-02 through 2026-07-28. The complete run used
  56 wrapper calls, zero retries, and produced immutable content-addressed
  payloads under ignored storage.
- Re-ran tactical v2.1 without network access. Recent and random older
  aggregates remained negative; only overlapping older month-end 20- and
  60-session aggregates were mildly positive. The conclusion is
  `MIXED_OR_UNFAVORABLE`.
- Added a strict Objective historical reconstruction preflight. It correctly
  stopped before scoring because the frozen cohort, discrete-quarter,
  period-start, historical market-value, and historical-membership evidence
  requirements are not satisfied.
- Added a separate approximate replay adapter for
  `LONG-HORIZON-RESEARCH-v1.0.0`. It calls the frozen evaluator without
  changing weights or thresholds and labels current-revision annual inputs as
  retrospective conservative-lag evidence.
- Fixed the long-horizon validation denominator at all 55 closed-universe
  securities, bound decision prices and derived assessment inputs into the
  snapshot hash, used raw close for historical price-to-shares valuation, and
  kept adjusted prices only for later return outcomes.
- Rejected non-USD, invalid-unit, and `REJECTED` facts; retained explicit
  limitations for `NOT_VERIFIED` provider facts, annual growth substitution,
  current-universe survivorship, overlapping outcomes, and unknown historical
  membership.
- The approximate long-horizon monthly holdout result is `UNFAVORABLE`.
  Across 73 older slices, top-minus-bottom spread was -2.53 percent at 126
  sessions and -4.18 percent at 252 sessions. Six older sealed random dates
  also produced negative spreads but remain below the 24-slice formal minimum.
- Added top- and bottom-bucket daily-close maximum-drawdown diagnostics. An
  independent review caught and rejected an earlier entry-relative MAE
  implementation before acceptance. The corrected running-peak calculation
  shows the high-score bucket had 2.73 and 3.23 percentage points deeper
  average drawdown at 126 and 252 sessions, so the model did not demonstrate
  downside protection either.
- No model formula, score weight, missing-data rule, database migration,
  provider request during evaluation, AI decision, commit, push, cloud
  resource, or deployment was introduced.

## Model validation v2, current-model freezes, and prospective contracts

- Added the Model Validation Master Plan v2 and governance contract with
  explicit claim ceilings, evidence roles, dependence handling, complete
  population accounting, and terminal `MIXED`, `NOT_VALIDATED`,
  `INSUFFICIENT_EVIDENCE`, and `BLOCKED_BY_DATA` states.
- Added one shared historical protocol with nested chronological folds, purge
  and embargo, non-overlapping formal schedules, separately labeled
  overlapping diagnostics, block-bootstrap support, six explicit benchmarks,
  liquidity-sensitive costs, turnover, coverage, drawdown, downside, and
  benchmark-relative metrics.
- Implemented and froze `TACTICAL-SIGNAL-v2.2.0`. It evaluates 1-week,
  1-month, and 3-month continuation and mean-reversion theses independently
  and applies explicit falling-knife, chase, volatility, liquidity, and
  event-risk gates.
- Implemented and froze `LONG-HORIZON-RESEARCH-v1.1.0`. It separates business
  quality, financial strength, capital allocation, valuation and entry,
  expected-return range, permanent-loss and downside risk, sector-relative
  evidence, and confidence. It deliberately has no default ranking score.
- Classified all previously observed Tactical v2.1 and Long Horizon v1.0
  results as `DEVELOPMENT_OBSERVED`; they are not untouched holdouts for the
  current models.
- Produced the Tactical v2.2 historical terminal as `BLOCKED_BY_DATA`, with 55
  `MISSING`, 2 `NOT_APPLICABLE`, and 9 `EXCLUDED` securities. Artifact
  SHA-256:
  `43FCFCFB4066BDFCF530308C8B04DDC409B6D6E6CFDB4DA0098424A9A207B7A0`.
- Produced the Long Horizon v1.1 historical readiness terminal as
  `BLOCKED_BY_DATA`, with the same 55/2/9 complete-population states and no
  computed score. Artifact SHA-256:
  `46352B1539D475F15ABA9B4E8CFE5D8E4E5D4E33AAB2313030578612F1563773`.
- Added local immutable Forward v2 decision-snapshot, preregistration,
  enrollment, and outcome contracts for 5, 20, 60, 126, and 252 completed
  sessions. At that contract-implementation checkpoint, no real post-freeze
  snapshot or matured outcome had been created; the later snapshot handoff is
  recorded below. Model quality remains `INSUFFICIENT_EVIDENCE`.
- Recorded that V16 is an append-only audit handoff only. A structured durable
  Forward v2 ledger requires a separately approved V18; no V18 was authorized
  or implemented.
- Performed this work without provider or other network requests, database
  migration, production API or UI integration, commit, push, cloud resource,
  scheduler activation, or deployment.

## First real local Forward v2 decision-snapshot handoff

- Assembled and sealed READY snapshot
  `beaa9952-9852-4088-9dc3-92047824414b` for universe
  `market-intelligence-closed-test-us-v1.0.0` at
  `2026-07-29T02:57:08.988871Z`.
- Preserved all 66 securities as 55 included, 2 reference-only, and 9 excluded
  records; no population member was dropped to improve readiness.
- Wrote controlled artifact hash
  `sha256:b00971fee0500a8d02f22e28b5402b8db36322127dc6500b6e354c60eb9d839c`
  and Git-safe manifest content hash
  `sha256:6afcfa078cafaa16dacf302d9cd71a63c586f0f1d8b5a157eaf7f0aab3247b30`.
- Recorded V16 audit event hash
  `sha256:eff628373f0c4a354cf761e30387713db1a2cb5acb41ce7fef61862a2e034542`;
  exact replay confirmed the same immutable handoff.
- Executed zero provider requests and preserved
  `aiUsedForDeterministicDecisions=false`.
- Stopped before enrollment because the only readiness blocker is
  `REQUIRED_BENCHMARK_EVIDENCE_UNAVAILABLE`. No Forward v2 outcome was created
  and model quality remains `INSUFFICIENT_EVIDENCE`.
- Added no V18 migration, public Forward v2 API or UI integration, commit,
  push, cloud resource, scheduler, or deployment.

## Formal Forward preregistration seal

- Added the immutable Forward DQV v2 and benchmark v2.1 preregistration
  writer/CLI.
- Bound Tactical v2.2 and Long Horizon v1.1, the 66 deterministic stable
  security identities, PIT/missing/freshness policy, five horizons,
  liquidity-sensitive costs, and all six formal benchmarks.
- Sealed the parent at `2026-07-30T03:12:23.237045Z` and the benchmark child at
  `2026-07-30T03:12:23.237053Z`; both are later than the model freezes.
- Wrote the parent, benchmark, and combined Git-safe seal under
  `docs/generated`; an exact second CLI execution preserved every file hash.
- Explicitly classified `beaa9952-9852-4088-9dc3-92047824414b` as
  pre-registration and prohibited its enrollment or upgrade.
- Added chronology, exact-hash, universe-identity, construction-policy,
  idempotent replay, immutable-conflict, legacy-boundary, and Git-safety tests.
- Executed no network request, scoring, database write, commit, push, or
  deployment.

## Objective benchmark coverage and lineage feasibility

- Added a read-only PostgreSQL and controlled-cache audit for Objective
  benchmark coverage, chronology, stable identity, and source lineage.
- Verified the formal 55-security denominator and effective minimum of 44.
- Found zero formal post-preregistration quality or value scores in the
  selected READY snapshot.
- Verified 32 pre-registration quality diagnostics (58.18%) and zero
  value-input-ready diagnostics. These records were not promoted.
- Verified 42 controlled included-security payload hashes and operand lineage,
  including 249 unique operand source hashes. The 32 quality diagnostics use
  192 unique hashes, none matched by the current PostgreSQL source-record
  ledger.
- Documented the V14–V17 reuse boundaries and V8 authoritative-score-ledger
  requirement.
- Recorded the independent sector-reference blocker: only SPY and XLK are
  reference-only members, which cannot satisfy the frozen per-sector benchmark
  construction requirement.
- Generated the Git-safe immutable audit artifact with file SHA-256
  `A9BE2C3CC3CFDDDC1C59C64F82EF8875C6188F123C29553DA3850E57D02610D0`
  and canonical hash
  `sha256:e81249cc0b12114f22e0737546f576a2539ada9d5888aa0c012c829076a89cf5`.
- Executed no provider request, score, formula/PIT change, database write,
  commit, push, or deployment.

## Forward benchmark v2.2 feasibility correction and preregistration

- Added the offline v2.2 feasibility audit, candidate policy, external
  reference universe, data preflight, preregistration, immutable seal, CLI,
  and regression tests.
- Verified the unchanged 66-security identity and role binding and the exact
  55-member included denominator.
- Verified 42/55 hash-bound current inputs for each mechanical value and
  quality rule; retained `DATA_PENDING` because the minimum is 44.
- Implemented the frozen valid-only top-quintile rule, 44-to-9 and 55-to-11
  selection counts, stable public-ID tie-break, and rejection below coverage.
- Added SPY and all 11 GICS sector ETF references without adding them to the
  evaluated population; SPY and XLK retain their existing public IDs.
- Wrote six immutable Git-safe artifacts and replayed the CLI exactly with
  unchanged hashes. The v2.2 seal canonical hash is
  `sha256:ed3e796290c3509c94429b7273346612c40f2b4db4b94889e0db7d583c7c8e0d`.
- Added no provider requests, values, benchmark results, database writes,
  commits, pushes, or deployments.

## Forward benchmark v2.2 input refresh

- Added a post-freeze EODHD Fundamentals refresh runner with an execution
  lease, physical-request journal, per-security checkpoints, immutable
  artifacts, explicit budgets, and zero provider retries.
- Executed the one authorized 55-security run
  `20260730T041722Z-02f8ddea2f6e`: 55 completed requests, configured weight
  550, zero failures, and zero retries.
- Preserved raw responses and numeric values only in Git-ignored
  content-addressed storage.
- Verified 55 valid pure-value inputs and 55 valid pure-quality inputs, passing
  the frozen 44-of-55 coverage gate for both candidate families.
- Constructed the frozen valid-only top-quintile candidate identities: 11 pure
  value and 11 pure quality. No deterministic model scores or outcomes were
  generated.
- Retained an earlier sandbox transport failure as an aborted journal with no
  completed provider response; it is not part of the successful construction.
- Recomputed the three Git-safe artifact hashes, 55 controlled-payload hashes,
  55 checkpoints, and 55 response hashes. The execution lease is released.
- Added no price requests, database writes, enrollment, commits, pushes, or
  deployments.

## Forward benchmark construction and successor readiness v2.2

- Added the v2.2 six-family construction adapter without modifying the
  accepted v1 or v2.1 implementations.
- Bound construction to the exact v2.2 preregistration, seal, 55-security
  Fundamentals artifacts, candidate sets, Future Price History v2 contract,
  external-reference universe, stable identities, action evidence, ADTV and
  frozen cost policy.
- Preserved the v2.1 mechanics for SPY, sector ETFs, equal weight and pure
  momentum. Added the frozen valid-only 44-of-55 mechanical value and quality
  candidate policies.
- Added a successor readiness controller that returns only `READY` or
  `BLOCKED` and cannot enroll, score, calculate outcomes, call providers or
  write PostgreSQL.
- Added complete-success fixtures and explicit tamper, binding, missing-price,
  missing-family, pre-freeze-decision, AI-contamination and V18 blocker tests.
- Generated the current-repository closeout as `BLOCKED` because completed
  price evidence, executed six-family construction, a post-freeze decision
  manifest and authoritative V18 acceptance evidence do not yet exist.
- Did not upgrade the legacy `beaa9952` decision or the old pre-implementation
  V18 audit.
- Executed no network request, database read/write, enrollment, score, outcome,
  commit, push or deployment.

## Forward DQV v2 outcome persistence V18

- Added `V18__create_forward_dqv_v2_outcome_ledger.sql` with seven
  append-only `analytics.*` tables.
- Added deferred five-maturity and complete-population/six-benchmark
  validation, stable public-security identity checks, exact gross/cost/net
  arithmetic, typed missing states, MAE/MFE/drawdown/downside metrics, and
  single-successor correction chains.
- Added the versioned Forward outcome v2.1 Pydantic contract without modifying
  v2.0 semantics.
- Added an atomic PostgreSQL repository for enrollment, outcomes, path metrics,
  and quality reports with exact replay, conflict rejection, and typed
  readback.
- Extended migration acceptance with an explicit V17-to-V18 upgrade path.
- Passed PostgreSQL 17 clean V1-to-V18 and V3/V12/V16/V17 upgrades.
- Passed real PostgreSQL repository replay, conflict, correction, and readback
  tests.
- Added no provider request, score, formula change, quality claim, commit,
  push, cloud resource, or deployment.

## Historical DQV v2.2 sealed-slice diagnostic

- Added a date-only slice-plan builder that verifies the existing Yahoo
  historical manifest and SPY payload hash without parsing OHLCV values before
  plan sealing.
- Sealed 18 seed-`20260729` stratified random anchors and nine fixed-offset
  anchors across five horizons. The plan canonical hash is
  `sha256:4e9f1d64b0ccf13c355c4cddcabda5a97a597cd97a0717c6b22c76015fce13e0`.
- Loaded outcomes only after the immutable plan was written and reverified.
- Produced 120 controlled benchmark path diagnostics with gross, cost, net,
  MAE, MFE, drawdown, downside deviation, downside capture, and coverage.
- Recorded SPY, current-universe equal weight, and pure momentum as available
  for all 120 matured anchor-horizon combinations.
- Kept dated sector, pure value, and pure quality explicit `MISSING`.
- Rejected 81 Tactical v2.2 and 39 Long Horizon v1.1 rows before scoring
  because required historical PIT evidence is unavailable.
- Retained `DEVELOPMENT_OBSERVED`, `DIAGNOSTIC_ONLY`,
  `formalGateEligible=false`, `untouchedHoldout=false`, and a terminal
  `CLOSED_WITHOUT_MODEL_VALIDATION` status.
- Stored derived licensed metrics only in Git-ignored controlled storage. The
  Git-safe closeout canonical hash is
  `sha256:c087760f8d0333ba2ab138cdf5b09a7f11e11f12336d5a7ac8abe10314e8f529`.
- Executed no provider request, database read/write, model score, parameter
  change, commit, push, or deployment.

## 2026-07-30 Gate H, statistics adapter, and Gate Z offline closeout

- Bound the accepted V19 chronology artifact and the v2.1.1 production write
  boundary into the current Forward DQV evidence graph.
- Added and accepted the offline Gate H maturity evaluator for the frozen
  5/20/60/126/252-session horizons, exact six benchmarks, gross/cost/net
  returns, MAE, MFE, drawdown, typed downside capture, volatility, and
  hash-rooted provenance.
- Added the exact-66 maturity-to-statistics adapter. It rejects fixtures,
  missing identity joins, silent imputation, and AI/human influence on
  deterministic results.
- Added the frozen statistical engine for deterministic circular block
  bootstrap, Holm correction, sector/size strata, Tactical timing analysis,
  Long expected-return calibration, and terminal quality classifications.
- Regenerated mutually consistent adapter and statistics preflights. Both
  remain blocked only by real evidence and natural-time conditions, not by a
  missing adapter implementation.
- Added the strict Gate Z human and machine-readable completion-gap audit. Its
  authoritative status is `CRITICAL_BLOCKED_NOT_VALIDATED`; it records offline
  implementation separately from time blockers, evidence blockers, actions
  not executed, and models not validated.
- Performed no provider request, database read/write, model score, enrollment,
  outcome observation, commit, push, or deployment.

## 2026-07-30 Repeated-date cohort controller v2.2

- Added a versioned offline controller and CLI for repeated prospective
  decision dates.
- Bound contract fixtures to the formal Forward DQV preregistration exact-66
  stable UUID population rather than synthetic identities.
- Added same-date exact replay, changed-hash conflict rejection, and
  cross-date enrollment/manifest/snapshot reuse rejection.
- Added per-security maturity evidence for the 5/20/60/126/252 horizons.
  Decision-time enrollment counts do not contribute to matured eligibility.
- Added 80% horizon coverage, 100 assessed decisions, two distinct dates,
  two-horizon matured span, and last-accepted-date purge/embargo checks.
- Added a future read-only PostgreSQL CLI path for v2.1.1 enrollments and
  latest outcome batches; no database was read during this task.
- Passed 13 focused controller tests and Ruff. The live CLI preflight stopped
  before writing an artifact because the immutable V19 acceptance source hash
  predates the additive due-maturity repository reader.
- Performed no network request, database write, real enrollment, outcome
  calculation, model run, commit, push, deployment, or cloud action.

## 2026-07-30 Deterministic decision-output binding v2.2

- Added an immutable exact-66 decision-output set produced by the real
  post-freeze Tactical and Long Horizon execution path.
- Preserved exact horizon results, typed missing states, stable UUIDs,
  chronology, classification hashes, row/input/result/evidence hashes, and
  direct model-freeze hashes.
- Bound the post-close orchestrator, decision snapshot, and maturity statistics
  adapter to the same sealed output set without formula reruns.
- Added immutable replay/conflict, future-evidence, source-hash, exact-66,
  missing-state, and adapter binding tests.
- Added a blocked preflight for unavailable real inputs and the missing
  controlled benchmark constituent/weight ledger.
- Executed no provider request, database operation, real score, enrollment,
  commit, push, or deployment.
