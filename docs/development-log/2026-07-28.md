# Development Log - 2026-07-28

## Market Intelligence and Screening v1

- Added `MARKET-INTELLIGENCE-SCREENING-v1.0.0` profile, eligibility, filtering,
  ranking, and internal HTTP contracts.
- Preserved Objective Rating v1 and existing tactical and long-horizon models
  as independent deterministic inputs.
- Added synthetic fixtures and tests for ranking, valuation, stale evidence,
  missing-state semantics, AI separation, and API serialization.
- Added a field-level persistence handoff without database migrations.

## Market Intelligence durable persistence integration

- Added a transactional PostgreSQL adapter over V17 and existing V1-V16
  security, snapshot, metric-observation, source-record, provider, ingestion,
  and classification identities.
- Added canonical content hashing, immutable idempotent profile/run writes,
  sealed read reconstruction, and strict AI narrative isolation.
- Added internal durable build, screen, profile-read, and result-read endpoints.
- Added schema-contract tests against the exact V1-V17 table and column names.

## Current-snapshot Objective Rating gate

- Added a current-decision-only EODHD/SEC factor-input adapter without changing
  Objective Rating v1 formulas or missing-data behavior.
- Preserved cached response hashes, provider paths, retrieval timestamps,
  filing dates, explicit scope labels, and controlled value storage.
- Added a day-weighted SEC fiscal-fourth-quarter diluted-share derivation.
- Built 216 current input snapshots; 190 had complete QC inputs and 136 passed
  formula-level economic validity.
- Ran the offline `QC-v1.0.0` gate for 136 mature operating companies.
- Kept historical PIT claims, UQ scoring, Forward Decision-Quality Validation,
  automatic trading, network requests, commit, push, and deployment out of
  scope.

## Forward Decision-Quality Validation preregistration

- Preregistered `FORWARD-VALIDATION-v1.0.0` in `DRY_RUN` mode after explicit
  approval.
- Froze the QC strategy path, 5/20/60-trading-day horizons, six shadow arms,
  USD 10,000 notional, cost model, cash-rate contract, sector ETF mapping, and
  enrollment rules.
- Verified a deterministic non-enrolled preview of 28 top-bucket and 28
  bottom-bucket securities from the 136-score baseline.
- Kept signals at zero until a fresh weekly-close score, verified trading
  calendar, benchmark/action coverage, and PIT cash rate are available.
- Preserved the original 300-to-500-security PIT requirement for `FORMAL`
  mode; current work is prospective dry-run evidence only.

## Forward enrollment operational preflight

- Verified that 2026-07-31 is a scheduled regular-session weekday against the
  published NYSE calendar while retaining a completed-session check at
  enrollment time.
- Verified complete sector ETF mapping for the current top/bottom preview.
  The required benchmark set is SPY, XLB, XLC, XLI, XLK, XLP, XLV, and XLY.
- Frozen the weekly-close sequence for fresh QC scoring, candidate and
  benchmark price/action refresh, Treasury cash-rate capture, signal sealing,
  and next-session shadow fills.
- Left enrollment not ready until those future weekly-close observations
  actually exist.

## Daily incremental Forward protocol

- Superseded the weekly operating cadence with
  `FORWARD-VALIDATION-v1.1.0` while retaining the weekly artifact as immutable
  historical evidence.
- Added per-security last-update state for prices, market capitalization,
  fundamentals, corporate actions, and identity.
- Added a deterministic daily refresh planner that queues only missing or
  stale datasets and cannot claim a pre-close session is complete.
- Reconciled the preliminary 582-request estimate to an authoritative
  589-request plan after the last-update scheduler correctly queued seven
  missing fundamentals caches. The final plan is approximately 0.589% of the
  100,000-call daily quota, with zero retries and explicit configured and hard
  ceilings.
- Kept live provider requests, refreshed scoring, signal enrollment,
  automatic trading, commit, push, and deployment out of this offline step.

## Tactical and long-horizon model separation

- Added `TACTICAL-SIGNAL-v1.0.0` with separate momentum and mean-reversion
  theses, entry confirmation, benchmark regime, liquidity, event drift, and
  volatility/drawdown/gap risk.
- Added deterministic one-week, one-month, and three-month states plus
  cost-aware walk-forward diagnostics. No diagnostic claims a proven
  statistical edge.
- Added `LONG-HORIZON-RESEARCH-v1.0.0` with separate general-company, bank,
  and recent-IPO handling.
- Added a cited evidence overlay bounded to plus or minus 10 points; it cannot
  fill missing data or determine a trade.
- Ran a bounded 13-request EODHD daily-price validation and a seven-response
  Fundamentals validation for AAPL, AMZN, TSLA, SPCX, UNH, GE, and NBN.
- Stored licensed raw payloads only in Git-ignored storage and retained
  content hashes in controlled evidence.
- Verified the full Python suite with 410 passing tests before the final
  documentation-only update.

## AI qualitative research contract

- Added `AI-EQUITY-RESEARCH-v1.0.0`, including fixed evidence dimensions,
  source-quality coefficients, CEO history and execution review, company
  strategy and dependency analysis, counter-thesis requirements, expiry rules,
  hard-risk outcomes and bounded long-horizon and tactical overlays.
- Added a reusable system prompt, input template and strict JSON output Schema.
- Set `gpt-5.6-terra` with medium reasoning as the default review
  configuration and reserved `gpt-5.6-sol` for material-risk escalation.
- Added per-security token, retrieval and application cost ceilings and
  required usage telemetry.
- Added a Git-safe ten-security demonstration artifact explicitly labeled
  `SIMULATED_NOT_SOURCE_VERIFIED`; it is not production evidence.

## Tactical Signal v2

- Preserved Tactical Signal v1 for reproducibility and added
  `TACTICAL-SIGNAL-v2.0.0` for new completed-daily-session evaluations.
- Separated momentum or mean-reversion setup type, rebound potential, reversal
  timing, trend confirmation, payoff asymmetry, risk, entry stage,
  actionability, and horizon outlook.
- Added early-candidate, limited-probe, confirmed, and invalidated semantics.
  A severe decline alone cannot authorize a probe.
- Added a one-session signal expiry, next-session-open execution boundary,
  threshold-margin stage confidence, and deterministic risk-unit caps.
- Added exact shared-session alignment and removed zero-volume pseudo sessions.
- Corrected the EODHD validation adapter to derive adjusted OHLC from
  `adjusted_close / close`; old split-crossing walk-forward results are not
  treated as V2 evidence.
- Added setup- and stage-specific walk-forward diagnostics with cooldown,
  costs, adverse gap-aware invalidation, MFE, MAE, and no statistical-edge
  claim.
- Made replay mode explicitly zero-request, removed its API-key requirement,
  recorded raw cache references and model provenance, and added a combined
  ten-security offline replay.
- Accepted the zero-request replay artifact
  `tactical-signal-validation-20260728T113936Z-all-requested.json` with file
  SHA-256
  `1A75EB70DA9CDAD83CEB463E061709596346A43370C65D73D11780CD020E8C0E`.
- Added the internal `POST /internal/v1/tactical/evaluate` FastAPI endpoint for
  caller-supplied adjusted completed daily bars. It performs no provider
  request and does not bypass the Spring-owned user-facing boundary.
- Kept live provider requests, automatic execution, portfolio-weight
  decisions, commit, push, and deployment out of this change.

## Tactical Signal v2.1 entry-value refinement

- Added `TACTICAL-SIGNAL-v2.1.0` without changing the long-horizon or
  mean-reversion contracts.
- Added deterministic momentum-extension risk and setup-specific current
  entry-value scores.
- Added `WAIT_FOR_PULLBACK` for a confirmed continuation thesis whose current
  completed-session price is too extended to accept as an entry.
- Kept a gradual 52-week breakout eligible when price structure is internally
  consistent; proximity to a high is not itself a penalty.
- Updated the sealed ten-security zero-request replay to v2.1 and fixed
  canonical serialization of integer lookback keys.

## Stable analytics model and provider interface

- Added exact model-ID and version routing for
  `LONG-HORIZON-RESEARCH-v1.0.0` and `TACTICAL-SIGNAL-v2.1.0`.
- Added stable request and result envelopes with timing, expiry, normalized
  input and evidence hashes, provider provenance, missing states and an
  explicit AI boundary.
- Added provider-neutral daily-price adaptation and a separate preassembled
  long-horizon evidence boundary.
- Added PIT guards for provider availability, retrieval time and tactical bar
  dates.
- Added versioned internal endpoints under
  `/internal/v1/analytics/models`.

## Forward Decision-Quality final offline acceptance

- Added a version-agnostic final acceptance runner bound to sealed source
  artifacts and immutable expected-contract hashes.
- Accepted framework integrity, next-session execution, 5/20/60-day horizons,
  six counterfactual arms, costs, slippage, cash, sector ETF and SPY baselines,
  abstentions, missing states and contamination controls.
- Recorded `PENDING_FRESH_SYNCHRONIZED_DECISION_SNAPSHOT` because the current
  objective and tactical artifacts are not a same-session full-coverage
  decision set.
- Recorded zero prospective completed episodes and
  `statisticalEdgeProven=NOT_ESTABLISHED`; historical diagnostics remain
  descriptive only.
- Made no provider request, database write, signal enrollment, commit, push or
  deployment during this offline acceptance.

## Stage verification corrections

- Fixed the mature-company gate defaults so its fixture, output directory and
  repository `.env` location do not depend on the caller's working directory.
- Re-ran the complete Python suite from both the repository root and the
  `analysis-python` CI working directory; both completed with 448 passing
  tests.
- Fixed the Windows Maven wrapper's null symlink-target handling and verified
  that it starts the cached Maven 3.9.16 runtime.
- Kept the backend test result unverified locally because the configured Spring
  Boot 4.1.0 parent POM was not available in the offline Maven cache. No
  previous report was presented as a fresh test.
- Independently verified the frontend lint/build and PostgreSQL 17 V1-V13
  empty/upgrade migration paths.
