# 2026-07-26: Analysis Product Design

## Product Clarification

- Defined stock assessment as an input to portfolio decisions rather than the
  final product output.
- Separated company quality, price attractiveness, near-term market condition,
  evidence coverage, and portfolio fit.
- Confirmed that a good company may be unsuitable at its current valuation or
  for a user's existing portfolio.
- Defined the future user context as investment goals, risk profile, sector
  preferences, cash, liabilities, leverage constraints, holdings, and allowed
  rebalancing scope.

## Investment Approach

- Adopted a Graham-inspired long-term discipline centered on financial
  strength, business quality, valuation, margin of safety, diversification, and
  human judgment.
- Distinguished defensive investing, enterprising investing, and explicitly
  limited speculation from investment horizon.
- Deferred a generic medium-term score until it has a distinct use case and
  testable methodology.
- Kept near-term market condition separate from long-term investment
  assessment.

## Screening Architecture

- Defined a two-stage research funnel:
  - Broad, deterministic quantitative screening
  - Source-backed AI evidence review for prioritized candidates
- Defined initial strategy paths as `Quality Compounder` and
  `Undervalued Quality`.
- Deferred `Emerging Growth` and specialized bank, insurer, REIT, resource,
  biotechnology, and special-situation models.
- Required comparison within sector, size, company-type, and strategy cohorts
  instead of a universal cross-company score.
- Required explicit coverage states so quantitatively scored, AI-reviewed,
  stale, insufficient-data, and specialized-model cases cannot be confused.

## Sector Design

- Separated sector classification from sector scoring.
- Defined an initial ETF-based sector market-condition assessment relative to
  `SPY`.
- Deferred sector fundamental attractiveness until a broad point-in-time
  constituent dataset is available.
- Prohibited use of the current six-security engineering set as evidence for a
  sector rating.

## AI Role

- Kept deterministic code responsible for calculations, eligibility, ranking,
  constraints, and backtesting.
- Assigned AI to structured evidence extraction, contradiction discovery,
  qualitative risk review, and cited explanation.
- Rejected unrestricted AI-generated numeric scores.
- Prioritized AI review for user holdings, explicit requests, watchlists,
  high-ranking candidates, material changes, and new filings.

## Data Strategy

- Confirmed that the existing Twelve Data integration remains useful for the
  current engineering slice and as a replaceable price provider.
- Selected SEC EDGAR as the preferred primary-source filing and XBRL evidence
  provider.
- Identified EODHD as the leading paid candidate for a one-month personal
  research validation, subject to field, point-in-time, delisted-data, and
  licensing acceptance tests.
- Deferred annual subscriptions and a final vendor commitment.
- Required a 20-security validation set before a 300-to-500-security
  stratified sample and broader United States coverage.

## Competitive Positioning

- Recognized that quantitative ratings, AI stock reports, and portfolio health
  tools already have mature competitors.
- Reframed the potential differentiator as a portfolio-aware, evidence-based
  decision and review workflow rather than another AI stock score.
- Identified the proposed distinguishing workflow as candidate discovery,
  adversarial evidence review, new-money and rebalancing comparisons, human
  decisions, and longitudinal thesis evaluation.
- Kept these advantages as hypotheses until demonstrated through working
  software and user validation.

## Next Workstream

The next dedicated analytics task should design and validate the first
quantitative methodology and its data contract before implementing a broad
production algorithm. The authoritative handoff is
[`docs/quantitative-screening.md`](../quantitative-screening.md).

## Objective Rating v1 Executable Validation

- Added immutable Python screening models, explicit Decimal factor formulas,
  deterministic cohort normalization, exact strategy contributions, rankings,
  separate horizon assessments, risk flags, missing reasons, and lineage.
- Added the fixed `QC-v1.0.0`, `UQ-v1.0.0`, and
  `NEAR_TERM-v1.0.0` configurations. Medium term returns `NOT_DEFINED`.
- Added a 100-observation deterministic cohort test, 20-security provider
  acceptance fixture, and six-security derived historical price fixture.
- Verified existing Twelve Data access for 162 adjusted daily observations per
  tested symbol from 2025-12-01 through 2026-07-24.
- Verified live SEC submission and XBRL lineage fields for Apple accession
  `0000320193-26-000013`.
- Added Python and Java screening-contract models and a shared JSON fixture.
  Java consumes the result without reproducing rating formulas.
- Added a provider validation matrix. No service was purchased, subscribed,
  deployed, committed, or pushed.
- Added a read-only provider acceptance CLI with SEC EDGAR and Twelve Data
  adapters, explicit `PASS`, `FAIL`, `NOT_VERIFIED`, and `NOT_APPLICABLE`
  states, and no raw-response persistence.
- Ran the representative `AAPL`, `META`, `GE`, `JPM`, `O`, and `TWTR`
  acceptance set. Five active symbols returned 1,648 adjusted daily
  observations each; AAPL split and dividend history passed; symbol-change,
  general split entitlement, and delisting-return coverage remain unverified.
- Required an explicitly configured SEC contact user agent. The tool never
  reads or transmits repository Git identity.

Validation commands:

```powershell
.\analysis-python\.venv\Scripts\python.exe -m ruff check analysis-python
.\analysis-python\.venv\Scripts\python.exe -m pytest analysis-python\tests -q
.\backend-java\mvnw.cmd test
```

The local PowerShell Maven wrapper launcher did not run correctly in this
environment, so the verified Maven 3.9.16 distribution already downloaded by
the wrapper was invoked directly. The Java test suite passed with that
equivalent Maven invocation.

## Complete Provider Acceptance Universe

- Completed the paced 20-security Twelve Data acceptance run.
- Verified adjusted daily history for 19 current securities; the former TWTR
  ticker remains `NOT_VERIFIED` and requires a delisted-security source.
- Confirmed that WMT and TGT dividend history and GE split history require
  entitlements beyond the current Twelve Data plan.
- Added an eight-second default request interval so validation respects the
  current eight-credit-per-minute limit.
- Added tests proving all 20 securities receive an explicit company-type gate
  and every requested corporate-action category is present in the report.
- Completed the 20-security SEC EDGAR acceptance run with 40 provider checks
  passing, seven explicitly unverified, six not applicable, and zero failing.
- Corrected the XOM acceptance identity to durable CIK `0000034088` after
  demonstrating that current ticker lookup can collide with an unrelated CIK.
- Confirmed that all ten mature-company cases have filing lineage; ADP and NKE
  still need validated operating-income mappings.
- Added timezone-aware historical SEC filing selection, supplemental submission
  index loading, and a hashed six-case AAPL/MSFT/META PIT fixture.
- Added adversarial tests showing that future filings and later amendments do
  not modify earlier filing selections.
- Added `TTM-YTD-BRIDGE-v1.0.0` and a hashed AAPL SEC fixture covering revenue,
  operating income, net income, operating cash flow, capital expenditure, FCF
  margin and cash conversion.
- Extended the AAPL PIT fixture with tax, pretax income, D&A, current/prior
  invested capital, shares and a bounded historical close, producing raw ROIC,
  leverage and valuation factors without emitting an incomplete score.
- Added a period-day-weighted TTM share bridge and used AAPL 2021/2024 SEC
  inputs to validate three-year EPS growth, FCF/share growth and dilution.
- Added a hashed 12-quarter AAPL fixture, cumulative-to-discrete derivation,
  margin stability and an explicit four-component margin-quality formula.
- Added a hashed 12-month AAPL PIT FCF-yield fixture and ascending historical
  midrank, with explicit 12/60 coverage.
- Confirmed from Apple's official 2024 Q2 inline XBRL that no compatible
  interest-expense tag is reported; interest coverage remains missing.
- Added a hashed MSFT/TGT cross-issuer TTM portability fixture. MSFT core and
  interest fields passed; TGT gross profit remains explicitly missing.
- Closed Objective Rating v1 executable method validation while retaining
  `NOT_ACCEPTED` for production full-market PIT data.

## Analytics Database Schema

- Added append-only Flyway migrations `V4` through `V9` for source lineage,
  durable security identity, immutable observations, PIT snapshots, universe
  membership, versioned factor and strategy definitions, screening tasks,
  coverage, ratings, contributions, and horizon assessments.
- Preserved the existing `analytics.daily_price` path and backfilled its rows
  into immutable observations with `NOT_VERIFIED` lineage.
- Added sealed-snapshot and completed-run guards plus separate
  `analytics_writer` and `analytics_reader` group roles.
- Added a PostgreSQL acceptance script for schema objects, strategy weights,
  legacy backfill, role isolation, missing values, immutability, and dual PIT
  cutoffs.
- Added a PostgreSQL 17 CI job that validates both clean installation and a
  representative `V3` upgrade with legacy price backfill.

## Historical SEC Point-in-Time Selection

- Added an immutable SEC fact observation contract with accession, filing,
  period, acceptance, availability, unit, and taxonomy metadata.
- Added deterministic historical fact selection using an explicit trading-date
  calendar and the first full United States session after filing acceptance.
- Added a frozen amendment fixture proving that a later restatement cannot
  change an earlier as-of result.
- Kept ADP and NKE operating-income coverage `NOT_VERIFIED`; no unsupported
  accounting substitution was introduced.
- Authorized read-only SEC evidence established same-accession operating-income
  derivations for ADP 2026 Q3 and NKE FY2026. Both use revenue less operating
  costs and an independent pretax/nonoperating cross-check.
- Added CIK-restricted derivation code and frozen public-fact fixtures. Mixed
  periods, missing components, unreviewed issuers, and disagreeing paths fail
  closed.
- Replayed all compatible companyfacts periods: ADP passed 59 of 59 and NKE
  passed 63 of 63 same-accession double-entry derivations. Frozen regression
  coverage now includes the latest case and two annual periods per issuer.
- Added an idempotent Python repository for normalized SEC facts. It resolves
  `security.public_id`, records provider/batch/source lineage, requires the
  source hash in the request identity, and inserts immutable facts without
  converting missing values to zero.
- Added Flyway `V10` so instant facts with a null period start remain
  idempotent under PostgreSQL `UNIQUE NULLS NOT DISTINCT`.

## Screening Pipeline Integration

- Added immutable Twelve Data price lineage alongside the legacy Phase 1
  projection, deterministic snapshot manifests, dated acceptance-universe
  metadata, PostgreSQL-backed screening tasks, advisory-lock recovery, atomic
  result persistence, and internal FastAPI snapshot/run/result endpoints.
- Added Spring Boot public screening endpoints that use only the versioned
  FastAPI HTTP contract, including idempotency-key forwarding and stable
  analytics-service failure mapping.
- Added shared Python/Java JSON fixtures for run creation, acceptance, status,
  ratings, and errors.
- Backed up the local V1-V3 database and upgraded the existing Docker volume
  through V10. All 10 Flyway migrations, the analytics schema acceptance
  script, the 96-row legacy price backfill, service readiness, and the existing
  six-security market-data API passed.
- Loaded all 20 reviewed acceptance securities. Live Twelve Data ingestion
  returned 251 daily rows each for AAPL, MSFT, META, WMT, MCD, CAT, ADP, and
  NKE before the provider limit rejected the remaining requests.
- Sealed a 20-member snapshot and completed two equivalent public-API screening
  runs with the same result hash. Coverage was 10 `INSUFFICIENT_DATA` and 10
  `SPECIALIZED_MODEL_REQUIRED`; no numeric rating was emitted because the
  accepted raw fundamental-to-factor coverage and minimum ranking cohort are
  not yet available. Missing inputs were not converted to zero.
- Replaced rating-page recomputation with reconstruction from immutable V8
  result tables and added risk-flag persistence. A 20-item public rating page
  had the same SHA-256 response hash,
  `25d3d454d58510c86b168148440b1c32c7fc221a062b917607d4c00fcbad652c`,
  before and after restarting the Python service.
# Forward Decision-Quality Validation v1

- Added immutable forward-validation domain models and a pure Decimal entry-policy
  and metric engine.
- Added V11 experiment, signal, policy-event, shadow-ledger, observation, metric,
  and report persistence with append-only result triggers.
- Added FastAPI experiment, enrollment, signal, observation, and report routes,
  with Java consumer contracts and a shared precision fixture.
- Added the English methodology, API contract, one-month template, two-month
  template, and architecture decision.
- Kept all experiments in `DRY_RUN` unless a formal provider acceptance ID is
  supplied. No scheduler, real trading, paid provider, commit, push, or
  deployment was introduced.
## User and Portfolio Foundation

- Added an English user and portfolio data-model specification.
- Added `V12` with separate users and authentication identities, versioned
  investment profiles, account and liability snapshots, aggregate portfolios,
  versioned constraints, scenario inputs and results, immutable decisions, and
  audit events under `app.*`.
- Added owner-preserving composite foreign keys and PostgreSQL acceptance tests
  that reject cross-user portfolio membership.
- Added sealed account snapshots whose cash and position children cannot be
  changed or extended after sealing.
- Added disabled-by-default closed-test identity resolution and `/api/v1/me`
  endpoints for accounts, snapshots, aggregate membership, and draft scenario
  input freezing.
- Added a parameterized local provisioning script for two opaque closed-test
  subjects; no private identity data is stored in the repository.
- Verified all migrations from an empty PostgreSQL 17 database and from the
  legacy upgrade path. Both analytics and application schema acceptance suites
  passed.
- Ran the Spring Boot test suite with Java 21: 14 tests passed.

## Investment Context and Portfolio Contract

- Added versioned investment-profile APIs with immutable goals and sector
  preferences.
- Added user-level and account-level liability APIs with immutable,
  idempotent balance snapshots and explicit aggregate-portfolio inclusion.
- Added user, portfolio, and account constraint-policy APIs.
- Implemented deterministic tightening rules: maxima can only decrease,
  minimum cash can only increase, and scenario resolution selects the strictest
  applicable values.
- Extended scenario creation to require and freeze the latest investment
  profile, all applicable constraint versions, complete account snapshots, and
  included liability balances.
- Defined `portfolio-calculation-v1` as a de-identified Java-to-Python contract
  and added a shared JSON request fixture. The endpoint remains inactive.
- Ran 22 Spring Boot tests successfully.
- Revalidated both PostgreSQL migration paths and all analytics/application
  schema acceptance checks.
- Completed an isolated end-to-end smoke workflow with two test identities:
  profile, account, sealed snapshot, aggregate portfolio, user liability,
  constraint policy, resolved constraints, and a draft new-money scenario.
  The scenario froze one input of each required type, generated a locked
  position permission, recorded ten audit events, and returned `404` for the
  second user's attempt to access the first user's portfolio.

## Consolidated Acceptance

- Python: Ruff passed and all 92 pytest cases passed.
- Java: all 22 Spring Boot tests passed on Java 21.
- Frontend: ESLint and the Next.js production build passed.
- PostgreSQL 17: empty-database and V1-to-V12 upgrade paths passed both
  analytics and application schema acceptance suites.
- No production data provider, authentication provider, brokerage connection,
  automatic execution path, or active portfolio-calculation endpoint was
  introduced.
