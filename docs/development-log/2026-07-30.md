# Development Log — 2026-07-30

## Frozen model validation terminal closeout

- Completed the Tactical v2.2 Tier 1 retrospective and deterministic
  block-bootstrap statistical closeout across 5-, 20-, and 60-session
  horizons.
- Recorded 5-session ranking as `NOT_VALIDATED`, 20- and 60-session ranking
  as constrained `PARTIALLY_SUPPORTED` diagnostic evidence, and every
  entry-timing horizon as `NOT_VALIDATED`.
- Completed Long Horizon v1.1 Tier 2 PIT reconstruction at 252, 504, 756, and
  1,260 sessions. No complete target input was reconstructed, so all Long
  Horizon validation targets remain `NOT_VALIDATED`.
- Added the versioned terminal closeout artifact and kept calibrated
  probability, portfolio suitability, AI deterministic influence, automatic
  trading, and observed-outcome retuning outside the accepted claims.
- Independently accepted the V20 append-only benchmark-outcome schema,
  prospective activation preflights, clean-clone test behavior, and strict
  secret-scanning configuration.

## Forward DQV V20 prospective activation

- Added a current-source-bound V19 v2 acceptance artifact without overwriting
  the historical V19 v1 acceptance.
- Added the V20 activation acceptance that binds the PostgreSQL successor
  schema, typed benchmark persistence, controlled six-family ledger,
  decision-controlled composite, human-decision governance, and portfolio
  boundary.
- Added versioned successors for the enrollment, post-close, deterministic
  output, and end-to-end gap preflights.
- Resolved only the stale benchmark-ledger implementation blocker. Current
  artifacts explicitly retain the missing real ledger, incomplete target
  session, missing 66-security model inputs, and unexecuted enrollment.
- Preserved model evidence labels, explicit missing states, PIT chronology,
  no-AI deterministic ranking, and no-trading boundaries.
- Added focused canonical-hash, source-drift, immutable-replay, supersession,
  and honest-state tests.
- Made no provider request, business database read or write, score, enrollment,
  outcome calculation, maturity execution, commit, push, or deployment.

## Forward DQV maturity discovery and stored-path loading

- Added a V19 production read port that discovers due v2.1.1 maturity
  schedules and reconstructs and verifies the enrollment and schedule hashes.
- Added the Forward DQV v2.2 maturity path loader for exact
  5/20/60/126/252-session stored price, action, calendar, and decision-time
  liquidity evidence.
- Added explicit `READY`, `PARTIAL`, `INVALID`, `NOT_DUE`, and
  `ALREADY_MATERIALIZED` assemblies for Gate H.
- Added exact-six-benchmark enforcement. SPY uses the stored security path;
  synthetic benchmarks remain explicit `MISSING` unless a sealed
  constituent/weight ledger is bound to the decision artifact.
- Added a future-compatible, hash-verified fixed-weight buy-and-hold benchmark
  ledger reader without creating or inferring a ledger.
- Added a manual read-only CLI, exclusive lease, immutable request/completion
  records, per-read checkpoints, exact replay, hash-drift rejection, and
  interrupted-run recovery.
- Added focused tests for not-due discovery, exact session and benchmark
  missing states, action and ADTV gaps, replay, hash drift, recovery, legacy
  enrollment rejection, materialized schedules, and 126-session
  diagnostic-only behavior.
- Added a Git-safe blocked preflight. No provider request, database write,
  model run, outcome calculation, commit, push, deployment, or cloud action
  occurred.

## Practical Tier-1 model-value validation

- Added a reusable benchmark evaluator with SPY-first comparisons, full
  assessable-universe equal weight where available, deterministic top and
  bottom portfolios, turnover costs, rank IC, hit rate, drawdown, and
  dependency-aware exploratory intervals.
- Executed frozen Tactical v2.2 scores against controlled historical outcomes
  without publishing provider-derived metric values in Git.
- Audited a deterministic 100-security Long Horizon source ledger without
  network requests and verified all controlled payload and raw Fundamentals
  response hashes.
- Executed Long Horizon v1.1 targets separately while preserving abstention,
  applicability, and evidence labels. Numeric results remain controlled local
  evidence.
- Corrected monthly valuation sampling, completed-session maturity,
  market-cap freshness, duplicate conflict handling, negative-pretax ROIC,
  stability history, score direction, non-overlapping primary anchors, and
  persisted artifact hash normalization.
- Independently accepted Tactical v2.2 as `PASS` and Long Horizon v1.1 as
  `PASS_WITH_LIMITATIONS`. No model weight was retuned from observed outcomes,
  and no provider request, AI ranking, commit, push, or deployment occurred.
