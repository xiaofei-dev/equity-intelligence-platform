# Development Log — 2026-07-27

## Mature-Company Gate Safety Hardening

- Did not make any EODHD or SEC network requests during this hardening step.
- Added a cross-process live-run lock that rejects a second mature gate while
  the first process holds the lock.
- Added unique run IDs and exclusive per-run report creation. Previous reports
  can no longer be overwritten by the CLI.
- Added `--maximum-symbols` for bounded canaries.
- Added a no-network preflight containing selected symbols, endpoint
  categories, local HTTP-request projections, and local weighted-call
  projections.
- Scaled the hard local request and weighted-call ceilings down to the selected
  canary size. A one-symbol canary cannot consume more than 10 EODHD attempts
  or 28 locally weighted EODHD calls; a retry consumes that same bounded
  allowance rather than expanding it.
- Required `--execute-live` together with the exact confirmation phrase
  `I_CONFIRM_BOUNDED_LIVE_REQUESTS`.
- Preserved sanitized provider error codes such as
  `EODHD_MALFORMED_RESPONSE` and `SEC_EDGAR_HTTP_429`.
- Added an offline regression fixture for the observed EODHD historical
  market-capitalization shape: a numerically keyed object whose records contain
  `date` and `value`.
- Added an offline fiscal-period alignment test for 52/53-week issuers.

## Usage Discrepancy Evidence

Confirmed:

- The only surviving report contains 120 successful EODHD Fundamentals
  requests, 120 EOD requests, 120 dividend requests, 120 split requests, and
  120 historical-market-cap requests.
- Under the local configured weights, that report accounts for 1,680 weighted
  EODHD calls.
- The same report contains no locally recorded EODHD retry attempts.
- The report file was created at 00:03 and modified at 00:10, consistent with
  the former CLI overwriting a shared output path.
- The EODHD dashboard reached approximately 12,000 calls during the day.

Not established:

- Visible Windows `python.exe` processes do not prove duplicate independent
  gate executions.
- The surviving report cannot reconstruct overwritten or interrupted runs.
- The local endpoint weights have not been reconciled to provider-dashboard
  billing.
- No root cause for the approximately 10,320-call difference is claimed.

The new lock is preventive and the billing discrepancy remains open.

## AAPL Billing Canary Evidence

- The approved AAPL canary used run ID
  `20260727T072655Z-8de2a32dbc1b`.
- The run made 10 physical EODHD attempts and three physical SEC EDGAR
  attempts, with no locally recorded retry or failure.
- Local configured endpoint weights totaled 28.
- The user-recorded EODHD dashboard counter moved from 11,324 to 11,374, an
  observed provider-billed delta of 50.
- The local model underestimated the dashboard delta by 22 calls. The observed
  delta was approximately 1.79 times the configured local weight.
- Provider billing reconciliation remains `NOT_RECONCILED`; the observation
  does not identify which endpoint or billing rule accounts for the
  difference.

## Revised Expansion Budget

Use a second network pass for no more than five securities. Replay the
already-normalized immutable payload for every other security to test
persistence idempotency without another provider request.

| Universe | EODHD HTTP | SEC HTTP | Local configured weight | Observed provisional billing | 1.5x safety budget |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 120 | 625 | 360 | 1,750 | 3,125 | 4,688 |
| 300 | 1,525 | 900 | 4,270 | 7,625 | 11,438 |
| 500 | 2,525 | 1,500 | 7,070 | 12,625 | 18,938 |

The observed provisional column treats a first pass as 25 provider-billed
calls and a complete two-pass sample security as 50. These are planning
assumptions derived from one AAPL observation, not reconciled endpoint prices.

## TGT, CAT, and JNJ Billing Canary Evidence

- The approved cross-sector canary used run ID
  `20260727T073440Z-3633dd8e5f40`.
- It made 30 physical EODHD attempts and nine SEC EDGAR attempts, with no
  locally recorded retries or failures.
- All three securities passed their complete gate coverage.
- Local configured endpoint weights totaled 84. The provisional provider
  billing estimate was 150.
- The user-recorded EODHD dashboard counter moved from 11,374 to 11,512, an
  observed provider-billed delta of 138.
- The observed delta was 54 above configured local weights and 12 below the
  provisional estimate. Provider billing reconciliation remains
  `NOT_RECONCILED`; this result does not identify endpoint-level billing.

## 120-Security Billing Evidence

- The bounded run used run ID `20260727T074150Z-50407538afa9`.
- It made 625 physical EODHD attempts: 120 first passes plus five bounded
  network reruns. SEC EDGAR made 358 attempts.
- Local configured EODHD weights totaled 1,750. The provisional provider
  billing estimate was 3,125 and the 1.5x safety ceiling was 4,688.
- The user-recorded dashboard counter moved from 11,512 to 14,387, an observed
  provider-billed delta of 2,875.
- The run contained exactly 125 complete EODHD passes, producing an aggregate
  observed rate of 23 calls per pass. This matches the TGT/CAT/JNJ canary:
  138 calls over six complete passes.
- The AAPL canary observed 25 calls per pass, so the conservative 25-call
  planning assumption is retained until more evidence is reviewed.
- The 2,875 delta was 1,125 above configured local weights, 250 below the
  provisional estimate, and 1,813 below the approved safety ceiling.
- Aggregate pass cost is increasingly reproducible, but endpoint-level billing
  remains `NOT_RECONCILED`.

## Focused Diagnostic Retest and Offline Remediation

- The focused run used ID `20260727T173753Z-079c976fda3f` for NVDA, EXPO, VZ,
  LANC, and TXN. It made 25 EODHD attempts and 13 SEC attempts without a
  second network fetch.
- The user-recorded EODHD dashboard delta was 115 calls, or 23 calls per
  complete provider pass. Run-level billing is `PROVISIONALLY_RECONCILED`;
  endpoint-level billing remains `NOT_RECONCILED`.
- NVDA and EXPO exposed a deterministic parser defect: `cash` contained a
  value while the lower-priority `cashAndEquivalents` alias was null, and the
  null overwrote the normalized `cash_and_equivalents` value. Parser
  `eodhd-parser-v1.2.0` now selects the first non-null value using a versioned
  alias-priority table. A later null can never overwrite a selected value.
- EXPO retains two annual period mismatches at 33 days. VZ retains three
  quarterly period mismatches at 91 days. The PIT tolerance remains exactly
  plus or minus seven days, so both cases remain incomplete pending a
  methodology decision or later filing evidence.
- LANC still lacks repository-retained authoritative CIK evidence. SEC ticker
  normalization now handles deterministic share-class punctuation, but no CIK
  override was invented.
- TXN completed all three SEC HTTP categories before failing in local PIT
  processing. Local session-availability and company-fact normalization
  failures now retain distinct sanitized codes instead of collapsing into
  `SEC_REQUEST_FAILED`.

## TXN PIT Availability Boundary and Offline Reclassification

- The TXN focused retest showed that ticker mapping, submissions, and
  company-facts requests all succeeded. Local selection failed because at
  least one eligible filing acceptance had no later complete trading session
  in the bounded price-date calendar.
- A filing without a post-acceptance complete session is now excluded from PIT
  fact selection instead of aborting the security. Older facts with defensible
  availability remain eligible. The exclusion records form, accession,
  acceptance timestamp, latest price trading date, as-of time, and the stable
  reason `SEC_NO_COMPLETE_TRADING_SESSION_AFTER_ACCEPTANCE`.
- The selector still requires an actual trading date after acceptance, never
  substitutes a natural day, never uses a future price date, and does not
  change the fiscal-period plus or minus seven-day rule.
- Offline derived run `20260727T175147Z-9f3b3532ca68` overlays only later
  immutable focused evidence onto the original 120-security report. It is not
  a new live gate. Confirmed PASS count is 72, leaving 28 additional confirmed
  passes required.
- Thirty-three securities remain field-diagnostic candidates because the
  original report did not retain normalized financial payloads. Their
  provider omissions versus alias/parser defects cannot be inferred without a
  bounded reparse.

## Cross-Run 100-Security Aggregate Acceptance

- Merged acceptance run `20260727T180044Z-2f1f1849e3a3` builds a
  latest-valid-result ledger over the original 120-security run and four
  subsequent immutable live runs.
- Every source report and diagnostic sidecar is verified against its declared
  SHA-256 before it can contribute evidence.
- Each symbol is counted once. Only a later hash-verified live result using the
  unchanged gate standard may replace an earlier status. Offline-derived
  evidence cannot upgrade a security.
- The ledger contains 120 unique securities and exactly 100 unique
  live-confirmed PASS records: 71 from the base run, one from the focused
  NVDA run, eight from the nine-security run, and 20 from the final bounded
  run.
- Aggregate gate status is `PASS`. This is explicitly a cross-run aggregate
  acceptance, not a new 120-security provider download or an authorization to
  run Objective Rating.
- Run-level billing is `PROVISIONALLY_RECONCILED` across the contributing
  dashboard intervals. Endpoint-level billing remains `NOT_RECONCILED`.

## Authoritative LANC SEC Ticker Repair

- An SEC-only evidence review confirmed Lancaster Colony Corporation, ticker
  `LANC`, under EDGAR CIK `0000057515`. The retained evidence references an SEC
  filing that identifies the issuer and exchange ticker; its EDGAR archive path
  carries CIK `57515`.
- The failed live snapshot had no canonical `LANC` match in
  `company_tickers.json`; ticker normalization and CIK zero-padding were not
  the cause. This is recorded as an official ticker-snapshot omission, not a
  guessed identifier.
- Added versioned `sec-authoritative-ticker-overrides-v1.0.0` fallback evidence.
  The ordinary SEC mapping remains primary when it agrees. A differing CIK is
  a hard conflict, and missing, expired, non-SEC, or unhashed entries are
  rejected.
- This repair does not change the PIT plus/minus seven-day rule, rating inputs,
  PASS criteria, or Objective Rating v1.
- Focused live run `20260727T185910Z-b514d27f39d3` confirmed that the
  authoritative mapping resolves the former CIK failure. LANC remained
  `PARTIAL` only because `activeStatus` was absent; PIT availability and all
  required rating fields passed.
- Frozen v2 slices 013 through 015 then completed independently with no retries
  or FAIL results. Their status distributions were respectively
  `16 PASS / 2 PARTIAL / 2 EXCLUDED`,
  `14 PASS / 4 PARTIAL / 2 EXCLUDED`, and
  `17 PASS / 1 PARTIAL / 2 EXCLUDED`.
- The final cross-slice v2 ledger contains 300 unique securities:
  243 `PASS`, 27 `PARTIAL`, and 30 `EXCLUDED`. All 243 PASS records have
  controlled-storage scoring-input receipts. This means provider/scoring-input
  coverage, not that Objective Rating was executed.
- Billing for runs without dashboard observations remains
  `PENDING_DASHBOARD`; endpoint-level billing remains `NOT_RECONCILED`.

## 300-Security Expansion Gate Closeout

- Final dashboard evidence reconciles the aggregate interval from `16480` after
  slice 003 to `21481` after all approved expansion work, an observed provider
  delta of `5001`.
- The interval contains exactly 14 executed runs: slices 004 through 015, the
  TSN replacement focused run, and the LANC authoritative-mapping focused run.
  Dry-run preflights and unexecuted slices are excluded. Slice 011 usage is
  retained because it consumed provider calls even though its invalid
  pseudo-security was later replaced.
- These runs recorded 1,086 EODHD attempts, 528 SEC attempts, 1,614 total HTTP
  attempts, 3,048 configured local weights, and zero retries. Their provisional
  provider total is 6,050 and aggregate hard safety ceiling is 9,076.
- The observed delta of 5,001 is below both limits, so run-level billing is
  `PROVISIONALLY_RECONCILED`. Endpoint-level billing remains
  `NOT_RECONCILED` because the dashboard does not allocate usage by endpoint or
  individual run.
- All 243 scoring-ready content-addressed payloads were read and their canonical
  hashes revalidated. Git artifacts contain manifests, hashes, and lineage but
  no licensed raw provider values.

## Formula Scoring-Input Contract Remediation

- The offline expansion Algorithm Gate correctly rejected all 243 provider
  PASS payloads. Provider coverage did not imply formula readiness: controlled
  payload v1 lacked gross profit, EBITDA, interest expense, diluted
  weighted-average shares, historical market capitalization, and price
  histories required by QC-v1.0.0 and UQ-v1.0.0.
- EODHD diagnostic field names confirm gross profit, EBITDA, and interest
  expense are provider-available but were omitted by the parser. The
  diluted-share aliases are defined deterministically, but their availability
  remains explicit per issuer because no such alias was observed in the
  retained diagnostics.
- Provider-neutral scoring-input v2 adds currency, observation type, provider
  schema, parser and normalization versions. It supports daily adjusted and
  unadjusted closes plus historical market capitalization with source hashes
  and conservative retrieval availability.
- Formula-history requirements remain frozen at a minimum eight aligned
  quarterly periods for stability factors and twelve historical valuation
  observations. Missing operands are listed explicitly and never become zero
  or neutral.
- Existing Git-safe artifacts did not retain the licensed values needed to
  rebuild these fields. A minimal 243-symbol plan therefore requests only
  fundamentals, EOD prices, and historical market capitalization. Existing SEC
  accession and PIT availability evidence is reused; no SEC network backfill is
  planned.
- The first three-symbol canary stopped during local AAPL normalization before
  CAT or JNJ. The provider returned legitimate periods outside the frozen v1
  PIT window, while the initial adapter incorrectly required an old accession
  for every extra period. The offline fix now ignores non-window periods but
  still rejects any missing period that belongs to the frozen PIT key set.
- The failed process did not persist telemetry, so exact physical attempts and
  provider billing are not claimed. Control flow bounds the reached AAPL work
  to at most three EODHD requests and 12 configured local weights. Billing
  remains `NOT_RECONCILED`, and no automatic rerun occurred.
- Dashboard evidence later measured that failed interval at 21 provider calls,
  below the 75-call provisional and 113-call hard ceilings. Run-level billing
  is provisionally reconciled, while endpoint allocation remains
  `NOT_RECONCILED`.
- The approved AAPL, CAT, and JNJ rerun completed with nine physical EODHD
  attempts, 36 configured local weights, and no retry. All three obtained price
  and historical-market-cap histories plus gross profit, EBITDA, and interest
  expense.
- None of the three provider payloads contained diluted weighted-average
  shares under the frozen aliases. AAPL also contained only one quarterly
  interest-expense period versus the required eight. All three therefore remain
  `INSUFFICIENT_DATA`; the 243-security backfill was not started.
- The EODHD canary dashboard reconciled from 21,502 to 21,565, an observed
  provider delta of 63 within the 75-call provisional and 113-call hard
  ceilings. Run-level billing is provisionally reconciled; endpoint-level
  billing remains `NOT_RECONCILED`.
- SEC Company Facts is now the authoritative supplement for diluted
  weighted-average shares. Versioned concept mapping preserves units, form,
  period/frame, accession, filing acceptance, PIT availability, semantic
  classification, and canonical source hash. Different concept semantics are
  not silently mixed, and YTD durations are not implicitly quarterized.
- SEC-only canary `20260727T201423Z-f94523bfc322` used exactly three ticker
  mapping, three submissions, and three company-facts requests, with zero
  EODHD requests and zero retries.
- AAPL, CAT, and JNJ now have respectively 36, 25, and 29 discrete diluted-share
  quarters. Their interest-expense histories contain 29, 8, and 30 quarters.
  All three controlled v2 payloads satisfy the frozen formula-field and history
  coverage gate. No score was calculated, and the 243-security backfill did not
  start.
- Formula-ready batch execution now separates a security-level terminal
  `SECURITY_INSUFFICIENT_DATA` result from `SYSTEM_EXECUTION_FAIL`. Exact
  reason codes retain observed and required counts, including
  `INTEREST_EXPENSE_QUARTERS_0_OF_8`; missing inputs remain absent and are
  never converted to zero or neutral values.
- Hash-verified insufficient-data checkpoints are resumable terminal evidence.
  A resumed batch skips the completed security without provider requests,
  while an execution-system failure still stops the batch.
- The version-compatible report reader labels legacy endpoint counts as logical
  evaluations rather than unverified physical requests. Version 1.1 reports
  keep logical evaluations, replayed endpoints, and new physical attempts
  separate.
- The deterministic remaining manifest excludes the three formula-ready
  securities and ABNB's completed insufficient-data result from the original
  243-symbol order. It contains 239 unique symbols in 12 immutable slices of at
  most 20, with no selection or replacement.
- Batch aggregates preserve exact status and reason counts. A system execution
  failure stops immediately; a single repeated data-gap reason affecting more
  than half of evaluated securities emits a systematic-gap stop signal for
  offline review. No provider request or Objective Rating execution occurred
  during this change.
- The controlled-evidence builder scans only immutable local scoring-input v2
  payloads, verifies each filename against its canonical content hash, and
  selects deterministically by latest record ingestion time and then content
  hash. It reports paths, receipts, formula coverage, EODHD base coverage, and
  SEC diluted-share and interest-expense window coverage without copying raw
  values into the Git-safe evidence artifact.
- The scan recovered complete local terminal evidence for A, ACN, ADBE, and
  ADI from interrupted or completed earlier work. All four are formula-ready
  with complete EODHD and SEC supplement coverage, so subsequent slice
  classification skips every provider endpoint for them.
- Twelve canonical frozen-slice artifacts now preserve the exact remaining
  manifest order and source hash. Endpoint planning uses the evidence flags to
  request only a missing EODHD or SEC endpoint group; corrupted evidence or
  manifest hashes are rejected before live execution.
- The final offline 243-security formula-input aggregate combines the four
  pre-slice terminal evidence records with all twelve completed slice reports.
  It verifies source/report canonical hashes, per-result checkpoints, immutable
  formula-ready storage filenames and payload hashes, receipts, formula
  coverage, exact insufficient-data reasons, and the absence of retries or
  system execution failures.
- The aggregate contains exactly 243 unique source-scope securities:
  223 are `FORMULA_READY` and 20 are
  `SECURITY_INSUFFICIENT_DATA`. This is an input-contract readiness result, not
  an Objective Rating run. The artifact contains hashes, counts, paths, and
  reason codes only; it excludes provider values and credentials.
- Across the twelve slices, telemetry records 705 new EODHD physical attempts
  and 476 new SEC physical attempts, for 1,181 total. Logical evaluations and
  replayed endpoints remain separate from physical attempts.
- Final dashboard reconciliation records 21,712 before the twelve slices and
  26,647 after them, an observed provider-billed delta of 4,935. The
  independently recomputed budget is 2,820 configured local weights, 5,875
  provisional provider calls, and an 8,813 hard safety ceiling.
- Run-level billing is `PROVISIONALLY_RECONCILED` because the observed delta is
  within the approved provisional and hard bounds. Endpoint-level billing
  remains `NOT_RECONCILED`; aggregate endpoint telemetry must not be presented
  as an EODHD billing-weight table.
- The immutable billing sidecar links the final aggregate, all twelve reports,
  preflight and completion journal events, 239 slice checkpoints, and all 243
  terminal payload/content hashes. It contains no provider values, licensed
  payloads, credentials, or scoring output.
- A content-addressed scoring-input v3 migration preserved all 223
  formula-ready v2 payloads without modifying v2. Each v3 record adds explicit
  period boundaries and duration evidence, derivation lineage, and the
  versioned market-availability policy. Classification snapshots seal sector,
  market-cap band, company type, applicability, universe version, as-of cutoff,
  and verified source hashes.
- The migration never treats a provider `QUARTERLY` bucket as proof of a
  discrete quarter or YTD value. Versioned subtraction requires matching
  taxonomy, unit, entity, fiscal period start, period chronology, and
  accession/availability chronology.
- The exact v3 coverage preflight remains `BLOCKED`: all 223 securities have
  unproven duration semantics, retained-source period starts are incomplete,
  and historical price/market-cap provider-publication times are unproven.
  Current-ranking and historical-PIT eligibility are therefore both zero. No
  score or network request occurred.
- Future SEC-only scoring supplements now use the PID/start-time/heartbeat
  `ExecutionLease` schema. Existing immutable SEC artifacts are unchanged.
- A separate value-free cache audit hash-verified 1,076 physical response
  checkpoints for the 223 formula-ready population. Cached EODHD and SEC
  transport evidence covers 216 securities; the seven earlier completed
  securities without these journaled transports are A, AAPL, ACN, ADBE, ADI,
  CAT, and JNJ.
- Cached SEC Company Facts contain period start/end, form, and accession
  metadata for seven already-approved normalized duration-field mappings.
  These are classified as `NEEDS_PARSER_EXTENSION`; no new SEC concept mapping
  was authorized. EODHD duration records have period ends but no period starts,
  forms, or accessions, so they remain
  `NEEDS_PROVIDER_DOCUMENTATION_OR_NEW_SOURCE`.
- Cached EOD and historical-market-cap responses contain observation dates but
  no provider-publication metadata. Repeating the same endpoints would not add
  the missing semantic fields, so the minimal live endpoint list is empty.
  Historical valuation remediation requires provider documentation or a new
  authoritative source, not an automatic live retest.
- Implemented `provider-neutral-scoring-input-v4.0.0` as a pure offline SEC
  timeline. The adapter verifies journal and response hashes, joins cached
  submissions acceptance timestamps to exact Company Facts accessions, keeps
  start/end/form/frame/unit/revision lineage, and writes values only to
  Git-ignored content-addressed storage.
- The final v4 manifest covers all 223 provider formula-ready securities:
  216 SEC timelines were built and seven remain
  `OFFICIAL_SEC_TIMELINE_NOT_CACHED`. The deterministic YTD bridge produced
  59,583 discrete-quarter derivations without a network request.
- The strict concept audit did not find
  `InterestExpenseNonOperating`. Broader interest concepts were deliberately
  rejected, debt components were not summed without non-overlap proof, EBITDA
  was not fabricated, and instant shares were not used before traded-class
  identity was proven.
- Current QC, current UQ, and historical PIT eligibility remain zero. This is
  an evidence decision, not a rating run. The Algorithm Gate and Forward
  Validation were not executed.
# SEC filing evidence route canary preflight stop

- Added the versioned, provider-neutral SEC filing evidence parser and bounded
  collector for official submissions, Company Facts, filing indexes, Inline
  XBRL primary documents, presentation linkbases, and label linkbases.
- The evidence contract preserves accession, form, filing and acceptance
  timestamps, contexts, dimensions, statement roles, presentation metadata,
  source references, source hashes, and parser/policy versions without placing
  filing values in Git-safe diagnostics.
- Interest scope remains strict: only
  `us-gaap:InterestExpenseNonOperating` can satisfy the approved gross
  non-operating-interest operand. Broad, debt-only, net, capitalized, and mixed
  concepts remain non-equivalent.
- Debt components remain unaggregated until issuer-specific non-overlap and
  completeness are proven. Instant shares remain unusable for market
  capitalization until the SEC filing evidence durably links the reported
  class to the requested traded symbol.
- The five-security SEC-only canary was bounded to AAPL, CAT, JNJ, NVDA, and
  XEL with 29 planned and 50 maximum physical HTTP attempts, zero retries, and
  zero EODHD requests.
- Execution stopped before network access because the local environment-path
  helper was called with an obsolete argument. The defect was fixed and
  regression-tested. Immutable failure evidence records zero physical
  requests and no acquired lock. The canary was not restarted automatically.
- After explicit approval, the identical five-security canary completed under
  run `20260728T035633Z-a0c07fd99aa9`: 29 SEC physical attempts, zero retries,
  and zero EODHD requests. All response journals and five controlled filing
  evidence payloads passed hash verification, and the execution lock was
  released.
- The filing evidence did not authorize an eligibility upgrade. None of the
  five filings proved the strict non-operating-interest concept, a complete
  non-overlapping total-debt family, or an unambiguous link between the
  instant-share fact and the requested traded common class. XEL exposed broad
  and debt-interest concepts, which remain explicitly non-equivalent.
- Official submissions and Company Facts were collected for AAPL, CAT, and
  JNJ. The remaining cache-missing symbols are A, ACN, ADBE, and ADI.
  Current QC, current UQ, and historical PIT eligibility remain zero pending a
  source/methodology decision; no score was generated.

# Frozen v1 source-semantics correction

- Audited the original Objective Rating v1 specification, factor code,
  provider mappings, validation documents, and later evidence decisions.
- Found that the v4.0 strict-interest rule was narrower than frozen v1 and
  used the nonexistent current-taxonomy spelling
  `InterestExpenseNonOperating`. The taxonomy concept is
  `InterestExpenseNonoperating`; the total concept is `InterestExpense`.
- Added `sec-interest-expense-policy-v1.1.0`. Consolidated unsegmented
  `InterestExpense` is compatible with the frozen absolute interest-expense
  denominator. `InterestExpenseDebt` and
  `InterestExpenseNonoperating` require issuer-specific completeness evidence.
  `InterestAndDebtExpense`, net interest, capitalized interest, segment facts,
  and component-only facts remain rejected.
- The offline cache contains accepted `InterestExpense` duration primitives
  for 171 securities. XEL's canary evidence is reclassified as an accepted
  total-interest primitive, but this does not make XEL or any strategy
  score-eligible.
- Confirmed that frozen v1 permits a standardized vendor total-debt field where
  supplied and treats EBITDA as a reported normalized input. EODHD fields are
  present for all 223 provider-formula-ready securities, but the public
  documentation does not define `shortLongTermDebtTotal` inclusion semantics
  or prove whether quarterly EBITDA is discrete, YTD, or TTM.
- Separated current snapshot, forward observation, and historical backtest
  requirements. Current market cap needs current-cutoff availability, not a
  past publication timestamp. UQ's historical FCF-yield factor still requires
  its frozen monthly PIT inputs, and a historical backtest still requires
  complete historical availability and revision lineage.
- Offline eligibility remains current QC 0, current UQ 0, and historical PIT
  0. No score, Forward Validation run, or financial-data network request was
  executed.

# EODHD documentation-only debt and EBITDA audit

- Retrieved and hash-recorded five official public documentation/OpenAPI
  resources. No EODHD or SEC financial data endpoint was called.
- Confirmed that EODHD labels `shortLongTermDebtTotal` total debt and defines
  financial-statement `ebitda` as `ebit + depreciationAndAmortization`.
- Kept total-debt equivalence unaccepted because company-varying composition,
  exhaustive exclusions, consolidation scope, instant semantics, and immutable
  revision lineage are not documented.
- Kept TTM EBITDA construction unaccepted because the quarterly collection is
  not defined as discrete-quarter, YTD, or TTM. Filing date and statement
  currency are documented, but acceptance time and field revision identity are
  not.
- Added a deterministic machine-readable audit with closed
  `PROVEN`/`NOT_DOCUMENTED`/`CONTRADICTED` decisions and regression tests that
  prevent field presence from authorizing semantic promotion.
- Drafted an English support inquiry without sending it. Current QC remains
  blocked; current UQ also retains the monthly PIT FCF-yield-history blocker.

# Current-only EODHD source-route correction

- Rechecked frozen Objective Rating v1 against the official EODHD glossary,
  debt explainer, and Fundamentals API documentation.
- Confirmed that frozen v1 accepts provider-normalized total debt and
  normalized reported/provider EBITDA. Accepted
  `shortLongTermDebtTotal` and `Highlights.EBITDA` TTM only for sealed current
  snapshots; did not use financial-statement quarterly EBITDA to manufacture
  TTM.
- Added a deterministic offline adapter that verifies physical-request and
  response hashes, validates Decimal values, currency, update metadata, and
  cutoff ordering, and writes licensed values only to Git-ignored
  content-addressed storage.
- Built 216 complete current-snapshot supplements from 223 target securities.
  A, AAPL, ACN, ADBE, ADI, CAT, and JNJ have no cached EODHD Fundamentals
  response and remain `INSUFFICIENT_DATA`.
- Recalculated source-contract coverage: 171 securities have accepted total
  SEC interest primitives, and 55 satisfy the complete primitive QC
  source-contract intersection.
- Kept algorithm eligibility at zero because current TTM, three-year, aligned
  eight-quarter, and required-factor status assembly was not executed. No
  score, cohort, rank, Forward Validation run, or financial-data API request
  was executed.

# Current factor-window methodology correction

- Audited the 55-security current factor-window assembler against frozen
  Objective Rating v1 formulas and the SEC v4.1 timeline.
- Kept adjacent `6M - 3M` and `9M - 6M` derivations under
  `SEC-YTD-DIFFERENCE-v1.0.0`. Added separately versioned
  `SEC-FY-MINUS-9M-v1.0.0` for strict annual-minus-nine-month fiscal Q4
  derivation.
- The Q4 rule preserves both accessions, hashes, availability timestamps and
  ordered lineage, and rejects amendment, revision, unit, context, cutoff,
  boundary, and fiscal-calendar conflicts.
- Produced 18,267 adjacent-YTD and 9,018 fiscal-Q4 derivations. Rejected 78
  amended combinations and two unproven 53/54-week alignments.
- Limited three-year endpoint assembly to margin quality, diluted EPS growth,
  FCF per diluted share growth, and dilution. Stability continues to require
  eight aligned quarters and is input-ready for 43 of 55.
- Added a current-only direct EODHD enterprise-value route because its official
  formula matches frozen v1. Missing minority interest is not zero; its
  component is `NOT_APPLICABLE` only when the direct enterprise-value route is
  selected.
- Full QC and UQ input readiness remain zero. Ten securities have all QC
  factor inputs except current accepted gross-interest TTM. UQ remains
  independently blocked by historical FCF-yield percentile.
- No live request, score, percentile, cohort, rank, Forward Validation run,
  commit, push, or deployment occurred.

# Bounded SEC issuer-interest evidence canary

- Executed the approved SEC-only AMAT, CSCO, and FIX canary over the 18 frozen
  accessions from the v1.3 issuer-consistency audit.
- Completed 72 physical requests: 18 each for filing indexes, Inline-XBRL
  primary documents, presentation linkbases, and label linkbases. Retries,
  EODHD, submissions, and Company Facts requests were zero.
- Preserved the live run under immutable run ID
  `20260728T051809Z-ff6c07bd66ff`; the cross-process lease was released.
- Found and versioned a deterministic parser correction: the SEC taxonomy uses
  `InterestExpenseNonoperating`, and `InterestExpense` is the preferred total
  gross-interest candidate rather than a mixed/net concept.
- Rebuilt all 18 filing-evidence records offline from hash-verified response
  journals using `sec-inline-xbrl-parser-v1.1.0`. No additional network request
  was made and the original report/evidence was not overwritten.
- Reassessment retained AMAT, CSCO, and FIX as `PARTIAL`. Presentation location
  and matching comparative values do not prove complete gross-interest scope
  or the absence of an omitted operating-interest component; calculation
  linkbases were outside the approved endpoint set.
- Generated no interest supplement, corrected factor snapshot, score, rank, or
  Forward Validation result. The evidence route stops without expanding to the
  remaining issuers.

# EODHD current-interest documentation and cache audit

- Read and hash-recorded five official EODHD public documentation resources;
  no EODHD or SEC financial-data endpoint was called.
- Confirmed that the official glossary defines `interestExpense` as borrowed
  funds cost and documents report currency, period-end date, and filing date.
- Kept the field unaccepted for frozen-v1 TTM interest coverage because the
  public contract does not define quarterly records as discrete, YTD, or TTM,
  does not explicitly define annual duration start, and does not close
  capitalized-interest, financing-fee, netting, or complete-scope semantics.
- Audited the 223-security source aggregate: 216 raw Fundamentals caches are
  hash verified and seven are absent. No cached Highlights, Valuation, or
  Technicals structure exposes an explicitly named TTM interest-expense field.
- For AMAT, CIEN, COO, CSCO, DHR, FAST, FIX, PLAB, TSN, and WDFC, controlled
  payloads contain eight quarterly and three annual interest records with SEC
  accession and hash lineage but no period start. All ten remain blocked.
- Extended the unsent EODHD support inquiry with precise interest scope,
  duration, unit, filing availability, revision, and version questions.
- Generated no supplement, corrected factor snapshot, score, rank, or Forward
  Validation result.

# Bounded Yahoo-EODHD current-interest cross-validation

- Implemented versioned Yahoo transport, interest normalization, and
  cross-provider comparison contracts for the frozen ten-security set.
- Persisted Yahoo responses and numeric differences in Git-ignored,
  content-addressed controlled storage. The immutable Git-safe report includes
  no provider values.
- Corrected an offline parser defect after Yahoo returned `meta.type` as a
  single-item array. The saved AMAT response was replayed; it was not requested
  again.
- Completed terminal run `20260728T075513Z-483a7026d70b` with nine Yahoo
  physical requests, one hash-verified response replay, zero EODHD requests,
  zero SEC requests, and zero retries.
- Classified six securities as `CROSS_PROVIDER_TTM_CONFIRMED`, three as
  `PROVIDER_VALUE_CONFLICT`, and CIEN as
  `YAHOO_INTERNAL_REVISION_INCONSISTENCY`.
- Existing local SEC evidence did not explain the CIEN, FIX, PLAB, or WDFC
  differences.
- Generated no interest supplement, score, rank, or Forward Validation result.

# Offline QC cohort completion feasibility audit

- Hash-verified the accepted v1.5 manifest, all 55 controlled factor snapshots,
  and the 48 SEC timelines belonging to QC-not-ready securities.
- Generated an exact per-security, per-factor, and per-operand blocker matrix
  with source, accession, period, and content-hash evidence but no provider
  values.
- Grouped the 48 securities into 17 exact blocker signatures and produced a
  provider-conflict-aware 13-security remediation ranking.
- Confirmed that all 48 retain an interest-coverage blocker. FIX, PLAB, and
  WDFC retain explicit provider conflicts; the other 45 have no accepted
  bounded current-interest confirmation.
- Found zero securities whose complete blocker set is fixable from existing
  approved cached evidence.
- Flagged direct diluted EPS, explicit current EODHD Highlights fields, and the
  150-day versus 200-day freshness-version discrepancy for Algorithm review.
- Made no network request, supplement, score, threshold change, Forward
  Validation run, commit, push, or deployment.
## 2026-07-28 — Offline QC current-input reassembly and residual planning

- Verified the accepted machine policy file SHA-256
  `FFFF26EB8FEA82B76BDAFBCFFD96F19BD1FEC668B902B5C04AD530E25C334FD7`
  and canonical hash
  `997F37C2BE1DBBD8A05F7AF49AD60B0928D56F012FB8F5FD6531AFCD68F74BBA`.
- Versioned the current factor assembler for the frozen 150-day limit and the
  three approved current-only EODHD fields.
- Reassembled all 55 controlled snapshots from immutable v1.5 inputs. Six are
  QC-input-ready; `CSCO` was removed by the corrected freshness rule.
- Generated an exact residual blocker matrix for the 14 conflict-free
  conditional targets.
- Produced a one-attempt, retries-zero Yahoo preflight for `TTC`, but marked it
  `DO_NOT_EXECUTE_COHORT_COMPLETION_PRECHECK_FAIL`: even perfect evidence would
  yield only 7 ready securities.
- Performed no provider request, scoring, supplement generation, Forward
  Validation, commit, push, or deployment.
