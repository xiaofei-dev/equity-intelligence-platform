import { canonicalId, springCsv } from "@/lib/portfolio-onboarding/mutations";

export async function POST(request: Request) { const input = await request.formData(); const accountId = String(input.get("accountId") ?? ""); const file = input.get("file"); if (!canonicalId(accountId) || !(file instanceof File) || file.size === 0 || file.size > 1_000_000) return Response.json({ code: "INVALID_CSV_PREVIEW_REQUEST", message: "Choose an account and a CSV file up to 1 MB." }, { status: 422 }); const form = new FormData(); form.set("file", file); const result = await springCsv(`/api/v1/me/accounts/${accountId}/snapshots/csv/preview`, form); return Response.json(result.ok ? result.payload : { code: result.code, message: result.message }, { status: result.ok ? 200 : result.status ?? 502 }); }
