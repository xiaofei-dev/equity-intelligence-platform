import type { OnboardingInventory } from "@/lib/portfolio-onboarding/backend";

export function OnboardingSummary({ inventory }: { inventory: OnboardingInventory }) {
  if (!inventory.ok) return <section className="rounded-2xl border border-amber-300/20 bg-amber-300/5 p-5"><h2 className="font-semibold text-amber-100">Onboarding unavailable</h2><p className="mt-2 text-sm text-slate-400">{inventory.error}</p></section>;
  return <section className="rounded-2xl border border-slate-800 bg-slate-900/40 p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-emerald-300/70">Owner-isolated onboarding</p><h2 className="mt-2 text-lg font-semibold text-white">Accounts and portfolios</h2><p className="mt-2 text-sm text-slate-400">Create data through Spring&apos;s manual or strict CSV snapshot APIs. Raw CSV bytes are never stored.</p></div><div className="flex gap-3"><Count label="Accounts" value={inventory.accounts.length}/><Count label="Portfolios" value={inventory.portfolios.length}/></div></div>{inventory.accounts.length === 0 ? <p className="mt-5 rounded-xl border border-dashed border-slate-700 p-4 text-sm text-slate-500">No accounts exist. Use the Spring onboarding API to create an account, then preview and commit a complete manual or CSV snapshot.</p> : <div className="mt-5 grid gap-3 md:grid-cols-2">{inventory.accounts.map((account) => <article key={account.id} className="rounded-xl border border-slate-800 bg-slate-950/45 p-4"><div className="flex justify-between gap-3"><p className="font-semibold text-white">{account.name}</p><span className="text-xs text-emerald-200">{account.accountType}</span></div><p className="mt-2 break-all font-mono text-xs text-slate-500">{account.id}</p></article>)}</div>}</section>;
}

function Count({ label, value }: { label: string; value: number }) { return <div className="rounded-xl border border-slate-800 px-4 py-2 text-center"><p className="font-mono text-lg font-semibold text-white">{value}</p><p className="text-[0.65rem] uppercase tracking-wider text-slate-500">{label}</p></div>; }
